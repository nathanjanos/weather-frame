# virtualenv (local Mac testing)
.venv/

# runtime image cache (re-downloaded on every run)
cache/

# speech models (auto-downloaded on first run with voice enabled)
models/

# python bytecode
__pycache__/
*.pyc

# local-only Claude Code settings
.claude/settings.local.json

# macOS
.DS_Store
